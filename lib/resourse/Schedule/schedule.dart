import 'package:flutter/material.dart';

class SchedulePage extends StatelessWidget {
  bool finished = false;
  SchedulePage({required this.finished});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
